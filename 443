# обробляємо кілька рядків вхідних даних
while True:
    try:
        numbers = list(map(int, input().split()))  
        if not numbers:                           
            break
        # циклічний зсув вправо
        numbers = [numbers[-1]] + numbers[:-1]
        print(*numbers)
    except EOFError:
        break
